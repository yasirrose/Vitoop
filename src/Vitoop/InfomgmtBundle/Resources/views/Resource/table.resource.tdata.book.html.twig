{% for book in resources %}
    <tr id="book-{{ book.id }}" class="vtp-{{ cycle(zebra, loop.index) }} ui-corner-all vtp-uiaction-list-showdetail{% include "VitoopInfomgmtBundle:Resource:table.resource.res.classhelper.listtagger.html.twig" %}">
        <td class="ui-corner-left">
            {% if (editMode is defined) and (editMode) %}
                <input type="number" id="coef-{{ attribute(coefficients, book.getID).rel_id }}" data-rel_id="{{ attribute(coefficients, book.getID).rel_id }}" min="0" data-original="{{ attribute(coefficients, book.getID).coefficient }}" max="120" step="0.1" value="{{ attribute(coefficients, book.getID).coefficient }}" class="vtp-uiaction-coefficient vtp-fh-w85"/>
            {% else %}
                {% if coefficients is defined %}
                    <input type="number" id="coef-{{ attribute(coefficients, book.getID).rel_id }}" data-rel_id="{{ attribute(coefficients, book.getID).rel_id }}" value="{{ attribute(coefficients, book.getID).coefficient }}" class="vtp-uiaction-coefficient vtp-fh-w85" disabled="disabled"/>
                {% else %}
                    {{ book.createdat|date("d.m.y") }}
                {% endif %}
            {% endif %}
        </td>
        <td title="{{ book.name }}">
            <div class="vtp-teasefader-wrapper">
                {{ book.name }}
                <div class="vtp-teasefader"></div>
            </div>
        </td>
        <td title="{{ book.author }}">
            <div class="vtp-teasefader-wrapper">
                {{ book.author }}
                <div class="vtp-teasefader"></div>
            </div>
        </td>
        <td>{{ book.tnop }}</td>
        <td>
            <div class="vtp-rating-image-small" title="{{ book.avgmark|avgmarkhint }}"
                 style="background-image: url('{{ asset('/img/rating/'~avgimg(book.avgmark)) }}');">
                <span>&nbsp;</span>
            </div>
        </td>
        <td>{{ book.res12count }}</td>
        {% if editMode is defined and editMode %}
        <td>{{ book.user.username }}</td>
        <td class="ui-corner-right">
            <span class="vtp-projectdata-unlink ui-icon ui-icon-close ui-corner-all" onclick="unlinkRes({{ book.id }})"></span>
        </td>
        {% else %}
            <td class="ui-corner-right">{{ book.user.username }}</td>
        {% endif %}

    </tr>
{% endfor %}